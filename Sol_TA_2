import turtle #To import the module named "turtle"

side = 100 #Set the variable length to 100

turtle.forward(side) #Make the turtle move in the forward direction by 100 pixels as side value is 100
turtle.left(90) #Make the turtle turn towards its left side by 90 degrees

turtle.forward(side) #Make the turtle move in the forward direction by 100 pixels as side value is 100
turtle.left(90) #Make the turtle turn towards its left side by 90 degrees

turtle.forward(side) #Make the turtle move in the forward direction by 100 pixels as side value is 100
turtle.left(90) #Make the turtle turn towards its left side by 90 degrees

turtle.forward(side) #Make the turtle move in the forward direction by 100 pixels as side value is 100
